# Architecture decision records

Copy `template.md` to the next zero-padded number when a ticket changes architecture, a trust
boundary, a public contract, a storage model, or a vendor integration.

Accepted ADRs are immutable. Supersede an earlier decision with a new ADR rather than rewriting its
history.
